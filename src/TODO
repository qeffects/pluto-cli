[ ] Limit rendered field sizes everywhere, max display limits:
 - description: 140
 - name: 40

[x] Add a "update" command

[x] Add a "lock" command           to lock a module at it's current version
[x] Add a "unlock" command         to unlock a module

[x] Add a "remove" command         to remove a module

[ ] Uri association? pluto-lua://

[x] Refactor commands from standalone modules to functions called by some index.mjs